name: otameshi

on:
  push:
    branches: ["main"]

jobs:
  job1:
    runs-on: [self-hosted, Windows, X64, label-1]
    steps:
      - name: Command実行
        run: |
          cd C:\next-fr
          py .\auto_test_driver.py -e 2
          echo "Hello World!"